version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jwt-backend
    ports:
      - "8000:8000"
    environment:
      # Replace with a strong random value before deploying to production
      SECRET_KEY: "supersecretkey-change-in-production"
    restart: unless-stopped
